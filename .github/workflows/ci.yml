name: CI

on:
  push:
    branches: [main, "model/**"]
  pull_request:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: ruff check src tests
      - run: pytest

  cold-blowout-ranker-backtest:
    needs: unit-tests
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - uses: actions/cache@v4
        with:
          path: |
            .cache/wyscout-repo
            .cache/football-data
          key: wyscout-live-dynamic-v1
      - name: Clone public Wyscout events
        run: |
          if [ ! -d .cache/wyscout-repo/.git ]; then
            rm -rf .cache/wyscout-repo
            git clone --depth 1 https://github.com/koenvo/wyscout-soccer-match-event-dataset.git .cache/wyscout-repo
          fi
          rm -rf .cache/wyscout-repo/processed
          ln -s processed-v2 .cache/wyscout-repo/processed
      - name: Run nested cross-league cold-blowout ranker
        run: |
          mkdir -p artifacts
          set +e
          python scripts/run_wyscout_cold_blowout_ranker.py \
            --data-root .cache/wyscout-repo \
            --workers 8 \
            --output artifacts/wyscout_cold_blowout_ranker.json \
            > artifacts/wyscout_cold_blowout_ranker.log 2>&1
          status=$?
          cat artifacts/wyscout_cold_blowout_ranker.log
          exit $status
      - name: Run strict unresolved cold-blowout audit
        run: |
          set +e
          python scripts/run_wyscout_cold_blowout_strict_audit.py \
            --data-root .cache/wyscout-repo \
            --workers 8 \
            --output artifacts/wyscout_cold_blowout_strict_audit.json \
            > artifacts/wyscout_cold_blowout_strict_audit.log 2>&1
          status=$?
          cat artifacts/wyscout_cold_blowout_strict_audit.log
          exit $status
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: wyscout-cold-blowout-ranker-report
          path: artifacts/
          retention-days: 14
